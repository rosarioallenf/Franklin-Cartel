# What Streamlit Cloud installs for the hosted app.
streamlit>=1.50
pandas>=2.0
openpyxl>=3.1
pdfplumber>=0.11
reportlab>=4.0,<5

# The hosted app keeps its data in Supabase, which is Postgres.
psycopg[binary]>=3.1
